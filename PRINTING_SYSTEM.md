# نظام الطباعة - Customer Ledger

## نظرة عامة

تم تطوير نظام طباعة احترافي لكشف حساب العميل يتجنب مشاكل Focus في Electron.

## المميزات

- ✅ طباعة آمنة بدون مشاكل Focus
- ✅ نافذة معاينة قبل الطباعة
- ✅ زر طباعة داخل النافذة
- ✅ استخدام Electron IPC APIs
- ✅ Fallback آمن لـ window.print

## الاستخدام

### طباعة فاتورة:
1. اضغط 🖨️ بجانب الفاتورة
2. تفتح نافذة تعرض الفاتورة
3. اضغط "🖨️ طباعة الفاتورة" داخل النافذة

### طباعة إيصال:
1. اضغط 🖨️ بجانب الدفعة
2. تفتح نافذة تعرض الإيصال
3. اضغط "🖨️ طباعة الإيصال" داخل النافذة

### طباعة كشف حساب:
1. اضغط "🖨️ طباعة الكشف" في الأعلى
2. تفتح نافذة تعرض الكشف الكامل
3. اضغط "🖨️ طباعة كشف الحساب" داخل النافذة

## البنية التقنية

### الملفات الرئيسية:

```
src/
├── services/
│   └── customerLedgerService.js    # منطق الحسابات
├── printing/
│   ├── invoiceTemplate.js          # قالب الفاتورة
│   ├── receiptTemplate.js          # قالب الإيصال
│   ├── ledgerTemplate.js           # قالب الكشف
│   └── safePrint.js                # وظيفة الطباعة الآمنة
├── components/
│   ├── CustomerLedgerModal.jsx     # المكون الرئيسي
│   ├── CustomerLedgerHeader.jsx    # الهيدر
│   ├── CustomerLedgerTable.jsx     # جدول المعاملات
│   └── TransactionActions.jsx      # أزرار الإجراءات
└── utils/
    ├── safeAlert.js                # تنبيهات آمنة
    └── safeConfirm.js              # تأكيدات آمنة

electron/
├── main.js                         # IPC handlers
└── print-preload.js                # Print window preload
```

### كيف يعمل:

1. المستخدم يضغط زر الطباعة
2. `safePrint()` يرسل HTML عبر IPC
3. `main.js` ينشئ BrowserWindow جديدة
4. النافذة تعرض المحتوى
5. المستخدم يضغط زر الطباعة داخل النافذة
6. `window.electronAPI.triggerPrint()` يرسل IPC
7. `webContents.print()` يفتح نافذة الطباعة

## التخصيص

### تعديل التصميم:

عدل القوالب في `/src/printing/`:
- `invoiceTemplate.js` - تصميم الفاتورة
- `receiptTemplate.js` - تصميم الإيصال
- `ledgerTemplate.js` - تصميم الكشف

### إضافة شعار:

```javascript
<div class="header">
  <img src="data:image/png;base64,..." alt="Logo" style="width: 100px;">
  <h1>اسم شركتك</h1>
</div>
```

## الأمان

- ❌ لا يستخدم `window.print()` في النافذة الرئيسية
- ❌ لا يستخدم `window.open()`
- ✅ يستخدم Electron IPC APIs
- ✅ نوافذ منفصلة ومعزولة
- ✅ Context isolation enabled

## المتطلبات

- Electron ^40.0.0
- React ^19.0.0
- Node.js >= 16

## الصيانة

عند تحديث Electron، تأكد من:
- توافق `webContents.print()` API
- عمل IPC handlers بشكل صحيح
- اختبار الطباعة على جميع الأنظمة

---

**آخر تحديث:** 2026-02-06
