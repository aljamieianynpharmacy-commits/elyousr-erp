# شرح إضافة أعمدة جديدة في جدول المنتجات

## النظام الجديد للأعمدة (Responsive Column System)

تم تحديث نظام الأعمدة ليكون **ديناميكياً وسريع التكيف** بدون الحاجة لـ scroll أفقي.

## كيفية إضافة عمود جديد

### الخطوة 1: إضافة تعريف العمود في `GRID_COLUMNS`

اذهب إلى ملف `src/pages/Products.jsx` وابحث عن `const GRID_COLUMNS = [`.

أضف عمودك الجديد بهذا الشكل:

```javascript
const GRID_COLUMNS = [
  // ... أعمدة أخرى ...
  {
    key: 'myNewColumn',           // معرّف فريد للعمود
    label: 'اسم العمود',          // العنوان المعروض
    width: 'minmax(80px, 1fr)',   // عرض مرن (موصى به)
    minWidth: '80px'              // الحد الأدنى للعرض
    // required: true             // اختياري: إذا كان ضرورياً دائماً
  },
  // ... باقي الأعمدة ...
];
```

### الخطوة 2: معالجة العمود الجديد في الـ Renderer

اذهب إلى دالة `renderGridCell` داخل component `ProductGridRow` (حول السطر 296).

أضف حالة جديدة في `switch` statement:

```javascript
const renderGridCell = (product, columnKey) => {
  // ... cases أخرى ...
  
  case 'myNewColumn':
    return (
      <div>
        {/* عرض بيانات العمود */}
        {product.myField}
      </div>
    );
    
  // ...
};
```

## نصائح مهمة

### 1️⃣ اختيار عرض العمود المناسب

**للأعمدة الصغيرة:**
```javascript
width: 'minmax(80px, 1fr)',    // أرقام، أيقونات
```

**للأعمدة المتوسطة:**
```javascript
width: 'minmax(100px, 1fr)',   // نصوص قصيرة
```

**للأعمدة الكبيرة (flexible):**
```javascript
width: 'minmax(150px, 1.5fr)', // نصوص طويلة
```

**للأعمدة الثابتة:**
```javascript
width: '180px',                // الإجراءات (ثابت)
```

### 2️⃣ متى تكون الأعمدة `required`

استخدم `required: true` فقط للأعمدة التي يجب أن تظهر دائماً:

```javascript
{ key: 'select', label: '', width: '52px', required: true },     // الاختيار
{ key: 'actions', label: 'إجراءات', width: '180px', required: true } // الإجراءات
```

### 3️⃣ الأعمدة المختيارية

جميع الأعمدة الأخرى تكون اختيارية ويمكن إخفاؤها من قائمة "الأعمدة":

```javascript
{ key: 'notes', label: 'الملاحظات', width: 'minmax(150px, 1.5fr)' }
// سيظهر تلقائياً في قائمة الأعمدة القابلة للإظهار/الإخفاء
```

## كيفية عمل النظام الجديد

### ✨ المميزات:

1. **Responsive**: الأعمدة تتقلص تلقائياً عند إضافة أعمدة جديدة
2. **No Horizontal Scroll**: لا يوجد scroll أفقي إلا إذا كانت الشاشة ضيقة جداً
3. **Flexible Widths**: استخدام `minmax()` يسمح للأعمدة بالتمدد والتقلص
4. **Toggleable Columns**: يمكن إظهار/إخفاء أي عمود (ما عدا الضرورية)

### 📊 شرح القيم:

```
minmax(min, max)
├── min: الحد الأدنى للعرض (لا تتقلص أقل من هذا)
└── max: عامل التمدد (كلما زاد الرقم، زاد المجال)

مثال: minmax(80px, 1fr)
├── 80px   = العرض لا يقل عن 80 بكسل
└── 1fr    = تأخذ حصة واحدة من المساحة المتبقية
```

## أمثلة عملية

### إضافة عمود للسعر الإجمالي

```javascript
{
  key: 'totalPrice',
  label: 'السعر الإجمالي',
  width: 'minmax(100px, 1fr)',
  minWidth: '100px'
}
```

### إضافة عمود للحالة

```javascript
{
  key: 'status',
  label: 'الحالة',
  width: 'minmax(90px, 1fr)',
  minWidth: '90px'
}
```

## 🚫 تجنب

- ❌ عدم إضافة `minWidth` - قد يسبب مشاكل التخطيط
- ❌ استخدام أعراض ثابتة فقط - سيسبب scroll أفقي
- ❌ عدم معالجة العمود في `renderGridCell` - سيظهر فارغاً

## 💡 نصيحة إضافية

إذا كنت تريد عمود جديد لكن بدون إضهاره افتراضياً:

```javascript
// اذهب إلى DEFAULT_VISIBLE_COLUMN_KEYS وأزل المفتاح
// DEFAULT_VISIBLE_COLUMN_KEYS سيتم حسابها تلقائياً من GRID_COLUMNS
// لكن يمكنك حفظ الاختيار في localStorage
```

---

**تم تحديثه:** فبراير 2026
**الحالة:** النظام الجديد لا يتطلب scroll أفقي ✅
